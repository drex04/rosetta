---
plan: 13-02
phase: 13
title: Example Project Wiring
status: not_started
depends_on: 13-01
must_haves:
  truths:
    - "Example Project" loads 3 sources: Norway (JSON), Germany (JSON), UK (XML)
    - Norway source has 5 pre-seeded direct mappings (trkNo, lat, lon, spd_kts, time)
    - SHACL shapes are loaded from the enriched sample-shapes.ttl
    - Build succeeds and npm run test passes
  artifacts:
    - path: src/components/layout/Header.tsx
      provides: updated handleExampleProject with XML source and enriched mappings
      contains: "xmlToSchema"
    - path: src/data/sample-source-c-uk.xml
      provides: imported by Header.tsx as ?raw
      contains: "<TrackReport"
---

# Plan 13-02: Example Project Wiring

## Tasks

### Task 1 — Import XML source in Header.tsx and add UK source
**File:** `src/components/layout/Header.tsx`

1. Add import: `import sampleUkRaw from '@/data/sample-source-c-uk.xml?raw';`
2. Add import: `import { xmlToSchema } from '@/lib/xmlToSchema';`
3. In `handleExampleProject`, after resultA and resultB, compute `resultC = xmlToSchema(sampleUkRaw, 'United Kingdom')`
4. Add third source entry `{ id: idC, name: 'United Kingdom', order: 2, rawData: sampleUkRaw, dataFormat: 'xml', schemaNodes: resultC.nodes, schemaEdges: resultC.edges, parseError: null }`
5. Update `useSourcesStore.setState` sources array to include all three sources

**Done:** `handleExampleProject` creates 3 sources; United Kingdom source uses `dataFormat: 'xml'`.

---

### Task 2 — Expand pre-seeded Norway mappings
**File:** `src/components/layout/Header.tsx`

After loading ontology and sources, seed 5 mappings for Norway source (direct kind):
1. `trkNo` → `nato:trackNumber`
2. `lat` → `nato:latitude`
3. `lon` → `nato:longitude`
4. `spd_kts` → `nato:speedKts` (already exists — keep and verify handle IDs)
5. `time` → `nato:timestamp`

Pattern: find source class node by URI (`http://src_norway_#RadarTracks`), find property by label, find ontology `nato:AirTrack` node, find target property by label. Add mapping with `kind: 'direct'`.

**Done:** After loading example project, Norway source in MAP tab shows 5 pre-populated direct mappings.

---

### Task 3 — Verify build and tests
Run `npm run build` and `npm run test`.

- If any test references the old SEED_TURTLE properties (`nato:speed` only — not `nato:speedKts`), update those test fixtures to use `nato:speedKts`.
- Fix any TypeScript errors from the new XML import.

**Done:** `npm run build` exits 0; `npm run test` exits 0.
