---
phase: 09-bug-fixes-ui-ux-overhaul
plan: 03
type: execute
wave: 1
depends_on: [02]
files_modified:
  - src/lib/shacl/instanceGenerator.ts
  - src/hooks/useOntologySync.ts
  - src/hooks/useSourceSync.ts
  - src/hooks/useAutoSave.ts
  - src/__tests__/layout.test.ts
  - src/__tests__/jsonldFramer.test.ts
  - src/lib/stringUtils.ts
  - src/lib/jsonToSchema.ts
  - src/lib/rdf.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "Empty schemaNodes in instanceGenerator returns an empty store — no bare-URI triples that evade SHACL validation"
    - "canvas-to-editor serialization failures surface as parseError in the editor (not silently swallowed in both useOntologySync and useSourceSync)"
    - "IDB restore failures set saveStatus to 'error' so the user sees 'Save failed' in the StatusBar"
    - "applyTreeLayout has unit tests covering empty input, single node, parent-child indentation, multiple roots, orphan placement, baseX offset, and property-height estimation"
    - "compactToJsonLd has integration tests covering empty store early-return, context from ontology URIs, standard prov/xsd prefixes, and first-wins dedup"
    - "toPascalCase and xsdRangeShort are exported from src/lib/stringUtils.ts; duplicate local definitions removed from instanceGenerator.ts and jsonToSchema.ts"
    - "STANDARD_NAMESPACES is unexported in rdf.ts; 4 unused devDeps removed; npm run build stays clean"
  artifacts:
    - path: src/lib/shacl/instanceGenerator.ts
      provides: "Empty uriBase guard preventing bare-URI triple generation"
      contains: "if (!uriBase) return store"
    - path: src/hooks/useOntologySync.ts
      provides: "canvas-to-editor error surfaced via setParseError"
      contains: "Canvas serialization failed"
    - path: src/hooks/useAutoSave.ts
      provides: "IDB restore failure sets saveStatus error"
      contains: "setSaveStatus('error')"
    - path: src/__tests__/layout.test.ts
      provides: "Unit tests for applyTreeLayout"
      contains: "applyTreeLayout"
    - path: src/__tests__/jsonldFramer.test.ts
      provides: "Integration tests for compactToJsonLd"
      contains: "compactToJsonLd"
    - path: src/lib/stringUtils.ts
      provides: "Shared toPascalCase and xsdRangeShort utilities"
      contains: "export function toPascalCase"
  key_links:
    - from: src/lib/stringUtils.ts
      to: src/lib/shacl/instanceGenerator.ts
      via: "import — replaces local toPascalCase/xsdRangeShort"
    - from: src/lib/stringUtils.ts
      to: src/lib/jsonToSchema.ts
      via: "import — replaces local toPascalCase/xsdRangeShort"

requirements:
  - REQ-79
  - REQ-82
  - REQ-84
---

<objective>
Close the gap analysis: surface three categories of silent failures (instanceGenerator bare URIs, canvas serialization errors, IDB restore errors), add missing unit/integration tests for layout.ts and jsonldFramer.ts, and remove dead code and unused dependencies.
</objective>

<context>
@src/lib/shacl/instanceGenerator.ts — empty uriBase fallback at line 35
@src/hooks/useOntologySync.ts — empty catch block at line 82
@src/hooks/useSourceSync.ts — empty catch block at line 125
@src/hooks/useAutoSave.ts — restore catch blocks at lines 79 and 94; setSaveStatus available in scope
@src/lib/layout.ts — applyTreeLayout, TREE_BASE_Y, TREE_INDENT_X constants
@src/lib/jsonldFramer.ts — compactToJsonLd, storeToNQuads
@src/lib/jsonToSchema.ts — toPascalCase and xsdRangeShort local definitions to be replaced
@src/lib/rdf.ts — STANDARD_NAMESPACES export at line 57 to unexport
@.planning/phases/09-bug-fixes-ui-ux-overhaul/09-CONTEXT.md — Plan 03 locked decisions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface silent failures — instanceGenerator guard, canvas serialization errors, IDB restore errors</name>
  <files>
    src/lib/shacl/instanceGenerator.ts,
    src/hooks/useOntologySync.ts,
    src/hooks/useSourceSync.ts,
    src/hooks/useAutoSave.ts
  </files>
  <action>
    **instanceGenerator.ts — empty uriBase guard:**

    At line 35, after `const uriBase = schemaNodes[0]?.data.prefix ?? ''`, add:
    ```ts
    if (!uriBase) return store
    ```
    This prevents `walkValue` from generating triples like `<Root>` with no namespace, which silently pass SHACL validation (shapes never match bare-name URIs).

    **useOntologySync.ts — surface canvasToTurtle failure:**

    Replace the empty catch block (lines 82–83):
    ```ts
    } catch {
      // Serialization failed — leave editor unchanged
    }
    ```
    With:
    ```ts
    } catch (err) {
      useOntologyStore.getState().setParseError(
        `Canvas serialization failed: ${err instanceof Error ? err.message : 'invalid node state'}`
      )
    }
    ```

    **useSourceSync.ts — surface sourceCanvasToTurtle failure:**

    Replace the empty catch block (lines 125–127):
    ```ts
    } catch {
      // Serialization failed — leave editor unchanged
    }
    ```
    With:
    ```ts
    } catch (err) {
      useSourcesStore.getState().updateSource(currentSourceId, {
        parseError: `Canvas serialization failed: ${err instanceof Error ? err.message : 'invalid node state'}`,
      })
    }
    ```
    `currentSourceId` is already in scope via `useRef` — confirm the variable name matches the existing code (it may be `sourceIdRef.current` or similar; read the surrounding context and use the correct reference).

    **useAutoSave.ts — IDB restore failures set error status:**

    The `setSaveStatus` setter is in scope throughout the hook. In the catch block at line 79:
    ```ts
    } catch {
      console.warn('rosetta: failed to restore project from IDB')
      setSaveStatus('error')
    }
    ```
    In the catch block at line 94:
    ```ts
    } catch {
      console.warn('rosetta: failed to restore sources from IDB')
      setSaveStatus('error')
    }
    ```
    Keep the `console.warn` lines — they help with debugging.
  </action>
  <verify>
    - `instanceGenerator.ts`: calling `jsonToInstances('{"x":1}', [])` returns an empty N3.Store (no quads)
    - `useOntologySync.ts`: `setParseError` is called (not swallowed) — readable in code review
    - `useSourceSync.ts`: `updateSource(..., { parseError: ... })` is called in catch — readable in code review
    - `useAutoSave.ts`: both catch blocks call `setSaveStatus('error')` — verify by code inspection
    - `npm run build` passes with no type errors
  </verify>
  <done>
    Empty schemaNodes returns empty store; canvas serialization failures surface as parseError; IDB restore failures set saveStatus to 'error'
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Unit tests for applyTreeLayout</name>
  <files>src/lib/layout.ts, src/__tests__/layout.test.ts</files>
  <action>
    Create `src/__tests__/layout.test.ts`. Import `applyTreeLayout`, `TREE_BASE_Y`, `TREE_INDENT_X` from `@/lib/layout`.

    Write tests covering:

    1. **Empty input** — `applyTreeLayout([], [], 0)` returns an empty Map
    2. **Single node, no edges** — placed at `{ x: baseX, y: TREE_BASE_Y }` (depth 0, first root)
    3. **baseX offset** — single node with `baseX=200` has `x === 200`
    4. **Parent-child indentation** — two nodes, one edge `{source: 'A', target: 'B'}`:
       - Node A placed at depth 0 (`x === baseX`)
       - Node B placed at depth 1 (`x === baseX + TREE_INDENT_X`)
       - Node B's `y` is greater than A's `y`
    5. **Multiple roots** — three nodes with no edges: each root is placed at depth 0, each subsequent root's Y is strictly greater than the previous
    6. **Orphan node** — a node not reachable from any root (simulate by having a cycle reference or a disconnected node):
       - After DFS places all reachable nodes, unreachable node still appears in the map
    7. **Property-height estimation** — a node with `data.properties` having 3 items should produce a larger Y gap for its sibling than a node with 0 properties:
       - `estimateNodeHeight` is not exported, but its effect is observable: two roots [A-with-3-props, B], B's `y` should be larger than [A-no-props, B]'s B `y`.

    Each test should be short (5–10 lines). Use `describe('applyTreeLayout', ...)` grouping.
  </action>
  <verify>
    - `npm run test -- layout` — all 7 cases pass
    - No imports of non-existent symbols (check export names in layout.ts first)
  </verify>
  <done>applyTreeLayout has unit tests covering all documented scenarios; npm run test passes</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Integration tests for compactToJsonLd</name>
  <files>src/lib/jsonldFramer.ts, src/__tests__/jsonldFramer.test.ts</files>
  <action>
    Create `src/__tests__/jsonldFramer.test.ts`. Import `compactToJsonLd` from `@/lib/jsonldFramer`.
    Import `N3` from `n3` to build test stores.

    Write tests (all async, using real jsonld.compact — no mocking):

    1. **Empty store early-return** — `compactToJsonLd(new N3.Store(), [])` resolves to `{ '@context': {}, '@graph': [] }` without calling jsonld (pure structural check)

    2. **Context from ontology nodes** — given an OntologyNode with `uri: 'http://example.org/ont#Track'`:
       - Context should contain key `'track'` mapped to `'http://example.org/ont#'`
       - Build a minimal store with one triple, pass the node, verify the context key exists in the result

    3. **Standard prov/xsd prefixes present** — with an empty ontology nodes array but a non-empty store:
       - Result `'@context'` should include `xsd: 'http://www.w3.org/2001/XMLSchema#'`
       - Result `'@context'` should include `prov: 'http://www.w3.org/ns/prov#'`

    4. **First-wins on duplicate context key** — two ontology nodes both with localName `'track'` but different base URIs:
       - Only the first node's prefix appears in context for the `track` key

    5. **Typed literal round-trip** — build a store with one quad:
       `_:b0 rdf:type ex:Track` where `ex:` = `http://example.org/ont#`
       - `compactToJsonLd` should resolve without throwing
       - Result should be an object (not null, not empty)

    Helper to build an OntologyNode stub:
    ```ts
    function makeNode(uri: string): OntologyNode {
      return { id: 'n1', type: 'classNode', position: {x:0,y:0},
               data: { uri, label: '', properties: [], prefix: '' } } as OntologyNode
    }
    ```
    Check OntologyNode type shape in `src/types/index.ts` and adjust fields as needed.
  </action>
  <verify>
    - `npm run test -- jsonldFramer` — all 5 tests pass
    - Tests are async and use `await expect(...).resolves` where appropriate
    - No mock of jsonld library — real compact call used
  </verify>
  <done>compactToJsonLd has integration tests covering all documented scenarios; npm run test passes</done>
</task>

<task type="auto">
  <name>Task 4: Dead code cleanup — extract shared utils, unexport STANDARD_NAMESPACES, remove unused deps</name>
  <files>
    src/lib/stringUtils.ts,
    src/lib/shacl/instanceGenerator.ts,
    src/lib/jsonToSchema.ts,
    src/lib/rdf.ts,
    package.json
  </files>
  <action>
    **1. Create src/lib/stringUtils.ts:**
    ```ts
    /** Capitalizes the first character of a string. */
    export function toPascalCase(key: string): string {
      return key.charAt(0).toUpperCase() + key.slice(1)
    }

    /** Returns a short XSD datatype string for a JavaScript primitive value. */
    export function xsdRangeShort(value: unknown): string {
      if (typeof value === 'boolean') return 'xsd:boolean'
      if (typeof value === 'number') {
        if (Number.isInteger(value)) return 'xsd:integer'
        return 'xsd:float'
      }
      return 'xsd:string'
    }
    ```

    **2. Update src/lib/shacl/instanceGenerator.ts:**
    - Add import: `import { toPascalCase, xsdRangeShort } from '@/lib/stringUtils'`
    - Delete the local `toPascalCase` function (lines 9–11)
    - Delete the local `xsdRangeShort` function (lines 13–20)
    - All existing call-sites in the file (`toPascalCase(key)`, `xsdRangeShort(value)`) continue to work unchanged

    **3. Update src/lib/jsonToSchema.ts:**
    - Read the file to confirm local definitions of `toPascalCase` and `xsdRangeShort` exist
    - Add import: `import { toPascalCase, xsdRangeShort } from '@/lib/stringUtils'`
    - Delete the local duplicate definitions
    - Verify all call-sites in the file still compile

    **4. Unexport STANDARD_NAMESPACES in src/lib/rdf.ts:**
    - Change line 57 from `export const STANDARD_NAMESPACES` → `const STANDARD_NAMESPACES`
    - If `STANDARD_NAMESPACES` is used within `rdf.ts` itself, that continues to work
    - If anything imports it (gap analysis says nothing does), the build will catch it

    **5. Remove unused devDependencies from package.json:**
    - Remove these four entries from `devDependencies`:
      - `"@testing-library/dom"`
      - `"@codemirror/commands"`
      - `"@codemirror/language"`
      - `"@codemirror/theme-one-dark"`
    - Do NOT remove `@codemirror/state`, `@codemirror/view`, or `@codemirror/autocomplete` — these may be used by the CodeMirror setup
    - After editing package.json, run `npm install` to update the lockfile
  </action>
  <verify>
    - `npm run build` — no TypeScript errors on any changed file
    - `npm run test` — existing tests pass (no broken imports from removed local functions)
    - `src/lib/stringUtils.ts` exists and exports both functions
    - `instanceGenerator.ts` and `jsonToSchema.ts` import from `@/lib/stringUtils` (no local copies)
    - `rdf.ts` line 57: `const STANDARD_NAMESPACES` (no `export` keyword)
    - `package.json` no longer lists the 4 removed devDeps
  </verify>
  <done>
    Shared utils extracted to stringUtils.ts; STANDARD_NAMESPACES unexported; 4 unused devDeps removed; build and tests pass
  </done>
</task>

</tasks>

<verification>
npm run build              # TypeScript clean across all changed files
npm run test               # All existing + new tests pass
npm run test -- layout     # layout.test.ts passes (7 cases)
npm run test -- jsonldFramer  # jsonldFramer.test.ts passes (5 cases)
</verification>

<success_criteria>
- Empty schemaNodes in instanceGenerator returns empty store (no bare-URI triples)
- canvas-to-editor serialization errors visible as parseError in editor UI
- IDB restore failures show 'Save failed' in StatusBar
- layout.ts fully tested (7 unit tests)
- jsonldFramer.ts fully tested (5 integration tests)
- No duplicate toPascalCase/xsdRangeShort definitions in codebase
- STANDARD_NAMESPACES is module-private; 4 devDeps removed; build clean
</success_criteria>

<output>.planning/phases/09-bug-fixes-ui-ux-overhaul/09-03-SUMMARY.md</output>
