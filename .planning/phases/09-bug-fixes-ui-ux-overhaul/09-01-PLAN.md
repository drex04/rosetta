---
phase: 09-bug-fixes-ui-ux-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/codemirror-theme.ts
  - src/hooks/useOntologySync.ts
  - src/hooks/useSourceSync.ts
  - src/lib/sparql.ts
  - src/lib/fusion.ts
  - src/store/ontologyStore.ts
  - src/components/canvas/OntologyCanvas.tsx
  - src/__tests__/sparql.test.ts
  - src/__tests__/codemirror-selection.test.ts
  - src/hooks/useInvalidateMappings.ts
  - src/__tests__/rdf-position.test.ts
  - src/__tests__/mapping-invalidation.test.ts
autonomous: true

must_haves:
  truths:
    - "Editing a source node name in .ttl preserves that node's canvas position"
    - "Opening Add Property from context menu does not leave the context menu visible behind the dialog"
    - "Double-clicking a word in any CodeMirror editor visually highlights the selection with a visible background color"
    - "Adding or renaming a node on the ontology canvas immediately updates the Turtle editor content"
    - "Renaming a DatatypeProperty URI in ontology .ttl removes mappings that referenced the old URI from the MAP tab"
    - "Running Transform & Fuse with direct mappings produces a non-zero triple count matching the source instance count"
    - "[review] AddPropertyDialog renders independently of nodeMenu state — closing context menu does not prevent dialog from appearing"
    - "[review] canvasToTurtle() errors during canvas→editor sync surface as Turtle validation errors in the editor status area"
    - "[review] executeAllConstructs skips sources with empty schemaNodes (guard against undefined schemaNodes[0])"
    - "[review] Editor is briefly read-only during canvas→editor debounce window to prevent overwrite races"
  artifacts:
    - path: src/lib/codemirror-theme.ts
      provides: "CodeMirror selection highlight styling"
      contains: "cm-selectionBackground"
    - path: src/__tests__/sparql.test.ts
      provides: "Tests for generateConstruct + fusion pipeline"
      contains: "direct mapping produces triples"
    - path: src/__tests__/mapping-invalidation.test.ts
      provides: "Tests for mapping invalidation on property rename"
      contains: "removeInvalidMappings"
    - path: src/__tests__/rdf-position.test.ts
      provides: "Tests for convertToSourceNodes position fallback on rename"
      contains: "convertToSourceNodes"
  key_links:
    - from: src/store/ontologyStore.ts
      to: src/hooks/useOntologySync.ts
      via: "Zustand subscribe for canvas→editor auto-sync"
    - from: src/store/ontologyStore.ts
      to: src/store/mappingStore.ts
      via: "Zustand subscribe for property URI diff → removeInvalidMappings"
    - from: src/lib/sparql.ts
      to: src/lib/fusion.ts
      via: "generateConstruct output consumed by executeAllConstructs"

requirements:
  - REQ-79
  - REQ-80
  - REQ-81
  - REQ-82
  - REQ-83
  - REQ-84
---

<objective>Fix 6 critical bugs: source node position jumps on .ttl edit, context menu/dialog overlap, CodeMirror selection invisibility, ontology canvas→editor sync gap, mapping invalidation on property rename, and Transform & Fuse producing 0 triples for direct mappings.</objective>

<context>
@src/hooks/useSourceSync.ts — source bidirectional sync (Bug 79)
@src/hooks/useOntologySync.ts — ontology bidirectional sync (Bug 82)
@src/lib/rdf.ts — convertToSourceNodes, parseTurtle, canvasToTurtle
@src/lib/codemirror-theme.ts — editor theme (Bug 81)
@src/components/canvas/OntologyCanvas.tsx — canvas + context menu wiring (Bug 80, 82)
@src/components/canvas/NodeContextMenu.tsx — context menu (Bug 80)
@src/components/canvas/AddPropertyDialog.tsx — property dialog (Bug 80)
@src/store/ontologyStore.ts — granular mutations, onInvalidateMappings (Bug 82, 83)
@src/store/mappingStore.ts — removeInvalidMappings (Bug 83)
@src/lib/sparql.ts — generateConstruct, derivePrefix (Bug 84)
@src/lib/fusion.ts — executeAllConstructs (Bug 84)
@src/lib/shacl/instanceGenerator.ts — jsonToInstances uriBase (Bug 84)
@.planning/phases/09-bug-fixes-ui-ux-overhaul/09-CONTEXT.md — locked decisions
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Fix CodeMirror selection visibility and context menu/dialog overlap</name>
  <files>src/lib/codemirror-theme.ts, src/components/canvas/OntologyCanvas.tsx</files>
  <action>
    1. In `src/lib/codemirror-theme.ts`, add selection styling rules to `lightTheme`:
       - `.cm-selectionBackground` with a visible blue background (e.g. `#b4d5fe`)
       - `&.cm-focused .cm-selectionBackground` with a slightly stronger blue
       - `.cm-cursor` with visible black color
    2. In `src/components/canvas/OntologyCanvas.tsx`, fix the context menu/dialog coupling:
       - Expand `addPropFor` state to include `nodeType` (currently only has `nodeId` and `nodePrefix`)
       - In `onAddProperty` callback (line 636), capture `nodeMenu.nodeType` into `addPropFor`, then immediately call `setNodeMenu(null)`
       - Change the dialog render condition from `{addPropFor && nodeMenu && (` to `{addPropFor && (`
       - Update `handleAddProperty` call inside dialog to use `addPropFor.nodeType` instead of `nodeMenu.nodeType`
       - This decouples the dialog from the menu — closing the menu no longer prevents the dialog from rendering
  </action>
  <verify>
    - Open any CodeMirror editor, double-click a word — selection has visible blue background
    - Right-click a node → "Add Property" → context menu disappears, dialog appears cleanly without overlap
  </verify>
  <done>Double-clicking a word in CodeMirror shows visible selection; Add Property dialog opens without context menu overlap</done>
</task>

<task type="tdd">
  <name>Task 2: Fix ontology canvas→editor sync</name>
  <files>src/store/ontologyStore.ts, src/hooks/useOntologySync.ts</files>
  <action>
    1. In `src/hooks/useOntologySync.ts`, add a `useEffect` that subscribes to `useOntologyStore` node and edge changes. When nodes or edges change AND `isUpdatingFromEditor.current` is false, call `canvasToTurtle(nodes, edges)` and update `turtleSource`. Use a ref flag `isUpdatingFromCanvas` to prevent the editor change handler from re-parsing.
    2. The subscription should use `useOntologyStore.subscribe` with a selector on `nodes` and `edges` (shallow compare length + IDs). Debounce at 100ms to batch rapid granular mutations (e.g. addNode followed by addPropertyToNode).
    3. Wrap the `canvasToTurtle()` call in a try-catch. On error, keep the last good editor content and surface the error as a Turtle validation message in the editor status area (e.g. "Sync error: missing URI on node X") so the user knows they're in an invalid state.
    4. During the 100ms debounce window, set the editor to read-only to prevent user edits from being overwritten by the pending canvas→editor sync. Restore editability after the debounce fires or is cancelled.
    5. Ensure the existing `onCanvasChange` callback still works for drag-position updates from React Flow's `onNodesChange`.
  </action>
  <verify>
    - Add a node via canvas context menu → Turtle editor immediately shows the new class
    - Rename a node via context menu → Turtle editor updates
    - Delete a node → Turtle editor removes it
    - Edit Turtle directly → canvas updates (existing behavior preserved, no infinite loop)
  </verify>
  <done>Adding or renaming a node on the ontology canvas immediately updates the Turtle editor content</done>
</task>

<task type="tdd">
  <name>Task 3: Fix source .ttl node position preservation</name>
  <files>src/hooks/useSourceSync.ts, src/lib/rdf.ts</files>
  <action>
    1. In `src/hooks/useSourceSync.ts`, inspect the `onSourceEditorChange` flow. After `parseTurtle(newTurtle)` produces new nodes, `convertToSourceNodes` is called with `existingSourceNodes`. The function at `rdf.ts:401` already maps positions by ID and URI.
    2. The likely issue: `parseTurtle` generates node IDs from class URIs (deterministic), but when a class is *renamed* in Turtle, the old ID disappears and a new ID appears — position is lost. Fix: in `convertToSourceNodes`, also match by **node index/order** as a fallback when neither ID nor URI matches. If the count of nodes hasn't changed and there's exactly one unmatched old + one unmatched new, carry the position over.
    3. Alternative (simpler): before calling `convertToSourceNodes`, build a position map keyed by node index in the existing array. Pass this as a third fallback in the position resolution chain: `posById → posByUri → posByIndex → default layout`.
  </action>
  <verify>
    - Open source .ttl editor, rename a class (e.g. `Track` → `TrackInfo`) — node stays in same canvas position
    - Add a new property to existing class — node position unchanged
    - Add entirely new class — new node gets default layout position, existing nodes stay put
  </verify>
  <done>Editing a source node name in .ttl preserves that node's canvas position</done>
</task>

<task type="tdd">
  <name>Task 4: Fix mapping invalidation on property rename</name>
  <files>src/store/ontologyStore.ts, src/store/mappingStore.ts, src/__tests__/mapping-invalidation.test.ts</files>
  <action>
    1. Create a dedicated `useInvalidateMappings` hook (new file `src/hooks/useInvalidateMappings.ts`), mounted in App.tsx. Subscribe to `useOntologyStore` node changes. On each change:
       a. Compute the current set of all property URIs across all ontology nodes
       b. Compare against the previous set (stored in a ref or closure)
       c. If any URIs disappeared (renamed or deleted), call `useMappingStore.getState().removeInvalidMappings(currentValidUris)`
    2. The subscription should fire after both canvas mutations AND editor→canvas sync. Use `useOntologyStore.subscribe` with a selector on `nodes` (specifically `nodes.flatMap(n => n.data.properties.map(p => p.uri))`).
    3. Write tests in `src/__tests__/mapping-invalidation.test.ts`:
       - Create ontology with property URI A, create mapping referencing A
       - Remove property A from ontology nodes → mapping is removed
       - Rename property A to B → mapping referencing A is removed
  </action>
  <verify>
    - Rename a DatatypeProperty in ontology .ttl → stale mappings disappear from MAP tab
    - Delete a property → its mappings are removed
    - Undo button in toast restores removed mappings
  </verify>
  <done>Renaming a DatatypeProperty URI in ontology .ttl removes mappings that referenced the old URI from the MAP tab</done>
</task>

<task type="tdd">
  <name>Task 5: Fix Transform & Fuse for direct mappings</name>
  <files>src/lib/sparql.ts, src/lib/fusion.ts, src/__tests__/sparql.test.ts</files>
  <action>
    1. Debug the namespace mismatch: `derivePrefix(sourceClassUri)` extracts namespace from the class URI. `jsonToInstances` uses `schemaNodes[0].data.prefix` as `uriBase`. These must produce identical strings.
    2. Add a diagnostic: in `executeAllConstructs` (fusion.ts), log the instance store quad count before running CONSTRUCT queries. If the store has quads but CONSTRUCT returns 0, the namespace mismatch is confirmed.
    3. Fix: In `executeAllConstructs`, pass the source's actual prefix (`source.schemaNodes[0]?.data.prefix`) to the CONSTRUCT query context OR fix `derivePrefix` to always produce the same prefix as the schema node. The cleanest fix: make `generateConstruct` accept an optional `sourcePrefix` parameter. When provided, use it instead of `derivePrefix(sourceClassUri)`. In `executeAllConstructs`, pass the source's prefix.
    4. In `executeAllConstructs`, add a guard: if `source.schemaNodes` is empty, skip that source with `console.warn` and continue to the next. This prevents a TypeError on `schemaNodes[0].data.prefix` for sources with no schema.
    5. Also check: the CONSTRUCT uses `?source` / `?target` variables. `?target` is unbound in WHERE — it becomes a blank node in CONSTRUCT results. This is correct SPARQL behavior. But verify Comunica handles this correctly by adding a test.
    5. Write/extend tests in `src/__tests__/sparql.test.ts`:
       - Generate CONSTRUCT for a direct mapping with known source prefix → verify PREFIX line matches
       - Run a direct mapping through `executeAllConstructs` with a simple JSON source → verify non-zero quad count
  </action>
  <verify>
    - Load sample project, run Transform & Fuse → "X triples from Y sources" shows non-zero counts
    - Direct mappings produce expected triple patterns
    - Existing sparql.test.ts tests still pass
  </verify>
  <done>Running Transform & Fuse with direct mappings produces a non-zero triple count matching the source instance count</done>
</task>

</tasks>

<verification>
- `npm run build` — zero TypeScript errors
- `npm run test` — all unit tests pass including new tests
- Manual: load sample project, edit source .ttl (rename class) → node positions preserved
- Manual: right-click node → Add Property → no menu/dialog overlap
- Manual: double-click word in Turtle editor → visible selection highlight
- Manual: add node on canvas → Turtle editor updates immediately
- Manual: rename ontology property in .ttl → stale mappings removed from MAP tab
- Manual: Transform & Fuse → non-zero triple count for direct mappings
</verification>

<success_criteria>
- Editing a source node name in .ttl preserves that node's canvas position
- Opening Add Property from context menu does not leave the context menu visible behind the dialog
- Double-clicking a word in any CodeMirror editor visually highlights the selection with a visible background color
- Adding or renaming a node on the ontology canvas immediately updates the Turtle editor content
- Renaming a DatatypeProperty URI in ontology .ttl removes mappings that referenced the old URI from the MAP tab
- Running Transform & Fuse with direct mappings produces a non-zero triple count matching the source instance count
</success_criteria>

<output>.planning/phases/09-bug-fixes-ui-ux-overhaul/09-01-SUMMARY.md</output>
