---
phase: 12-onboarding-demo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/about-dialog.tsx
  - src/components/layout/AppLayout.tsx
  - src/components/layout/Header.tsx
  - e2e/about-dialog.spec.ts

must_haves:
  truths:
    - "Clicking 'About' in the header opens the onboarding dialog"
    - "First-time visitors see the dialog auto-open on page load"
    - "Dialog shows 5 slides with prev/next navigation and animated transitions"
    - "Progress dots are clickable and reflect the active slide"
    - "Closing via 'Get Started', 'Skip', or the X button sets localStorage rosetta-onboarding-v1=seen and prevents future auto-opens"
  artifacts:
    - path: src/components/ui/about-dialog.tsx
      provides: "Self-contained onboarding dialog with slide navigation, SVG visuals, and close callback"
      contains: "SLIDES"
    - path: src/components/layout/AppLayout.tsx
      provides: "First-visit localStorage check and open state management"
      contains: "rosetta-onboarding-v1"
    - path: src/components/layout/Header.tsx
      provides: "About button wired to open callback"
      contains: "onAboutClick"
    - path: e2e/about-dialog.spec.ts
      provides: "E2E coverage for auto-open, slide nav, close behaviours"
      contains: "rosetta-onboarding-v1"
  key_links:
    - from: src/components/layout/AppLayout.tsx
      to: src/components/ui/about-dialog.tsx
      via: "renders <AboutDialog open={isOpen} onClose={...} />"
    - from: src/components/layout/AppLayout.tsx
      to: src/components/layout/Header.tsx
      via: "passes onAboutClick={() => setIsOpen(true)} prop"

requirements:
  - REQ-72

autonomous: true
---

<objective>
Build the Rosetta onboarding dialog — a 5-slide modal with SVG illustrations that auto-opens on first visit and is re-openable from the About button in the header. Covers problem statement, semantic web approach, technology stack, workflow, and a call-to-action slide.
</objective>

<context>
@src/components/layout/AppLayout.tsx
@src/components/layout/Header.tsx
@src/components/ui/confirm-dialog.tsx
@.planning/phases/12-onboarding-demo/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AboutDialog component</name>
  <files>src/components/ui/about-dialog.tsx</files>
  <action>
Create `src/components/ui/about-dialog.tsx` as a self-contained component.

**Props interface:**
```ts
interface AboutDialogProps {
  open: boolean;
  onClose: () => void;
}
```

**Slide data (SLIDES const array) — 5 entries:**
1. tag: "THE CHALLENGE", title: "Your Data Doesn't Speak the Same Language", body: "Air defense depends on radars, ESM sensors, C2 platforms, and intelligence databases — each with its own format and vocabulary. Today, analysts manually cross-reference tracks and reports to build the picture. It's slow, brittle, and incomplete.", visual: SilosVisual, accent: "red"
2. tag: "THE SOLUTION", title: "A Shared Ontology Changes Everything", body: "An ontology is a formal, machine-readable description of concepts — Aircraft, Track, Threat, Sensor — and how they relate. When every system maps its data to this shared model, correlation happens automatically. No custom code. No analyst effort.", visual: OntologyVisual, accent: "teal"
3. tag: "THE TECHNOLOGY", title: "Built on Proven, Open Standards", body: "RDF stores data as simple triples (Subject → Predicate → Object). OWL defines the ontology rules. SPARQL lets you query across everything. These are W3C standards — vendor-neutral, widely adopted in defence and intelligence, and future-proof.", visual: StandardsVisual, accent: "blue"
4. tag: "THE RESULT", title: "New Sources Online in Days, Not Months", body: "Instead of building N×(N-1) point-to-point connectors, each source maps to one shared model. Onboarding a new feed is a four-step process: Map → Connect → Validate → Query. What used to take months now takes days.", visual: SpeedVisual, accent: "amber"
5. tag: "TRY IT NOW", title: "See Semantic Fusion in Action", body: "Load real JSON and XML data sources, map them to a shared air defense ontology, and watch them fuse into a unified graph. Explore how ontology-driven integration delivers instant cross-source correlation — all running locally in your browser.", visual: DemoVisual, accent: "teal"

**Accent color map** (used for tag badge + active dot):
- red: `bg-red-500/10 text-red-400`
- teal: `bg-teal-600/10 text-teal-400`
- blue: `bg-blue-500/10 text-blue-400`
- amber: `bg-amber-500/10 text-amber-400`

**SVG components** — define all 5 as named functions in the file (adapted from reference, using the same viewBox and shapes but with Tailwind-compatible fill values):
- `SilosVisual`: 4 colored boxes (Radar/ESM/C2/Intel) with ✕ marks between them
- `OntologyVisual`: central AirTrack circle with dashed lines to Radar/ESM/Threat/EngageZone nodes
- `StandardsVisual`: 4 stacked color-coded layers (SPARQL / OWL+SHACL / RDF Triples / URIs)
- `SpeedVisual`: numbered 1→2→3→4 steps (Map/Connect/Validate/Query) with Before/After time comparison
- `DemoVisual`: JSON + XML sources converging through Ontology Mapping box into a Unified COP panel

**Component state:**
```ts
const [slideIndex, setSlideIndex] = useState(0);
const [animState, setAnimState] = useState<'enter' | 'exit'>('enter');
```

**animateToSlide(i):** set animState='exit', setTimeout 250ms → setSlideIndex(i), setAnimState('enter'). Reset to slide 0 when `open` changes from false→true (useEffect on `open`).

**Layout (DialogContent, className="max-w-3xl p-0 gap-0 bg-slate-900 border-slate-700"):**
- Top content area (p-8 pb-0, min-h-[340px]):
  - Tag badge: `inline-block px-2.5 py-0.5 rounded text-xs font-bold tracking-widest uppercase mb-4` with accent classes
  - Two-column flex row (gap-7):
    - Left: title (`text-2xl font-bold text-slate-50 mb-3 leading-tight`) + body (`text-sm text-slate-400 leading-relaxed`)
    - Right: SVG box (`w-52 h-40 shrink-0 rounded-lg bg-slate-800 border border-slate-700/50 p-2 flex items-center justify-center`)
  - Wrap entire content area in animated div: `style={{ opacity: animState==='enter' ? 1 : 0, transform: animState==='enter' ? 'translateY(0)' : 'translateY(10px)', transition: 'opacity 0.25s ease, transform 0.25s ease' }}`
- Footer (px-8 py-5 flex justify-between items-center border-t border-slate-800):
  - Left: progress dots — map slides to dots; active dot `w-5 h-2 rounded-full bg-teal-500`, inactive `w-2 h-2 rounded-full bg-slate-700 hover:bg-slate-500`; all `cursor-pointer transition-all duration-300`; onClick calls animateToSlide(i)
  - Right: button row (gap-2.5 flex items-center):
    - "Skip" (ghost, text-slate-500, size="sm") — visible on all slides except last; calls onClose
    - "Back" button — visible when slideIndex > 0; variant="outline", size="sm", border-slate-700
    - Next/Get Started button — variant="default" size="sm" bg-teal-600 hover:bg-teal-700; label "Next" or "Get Started" on last slide; on last slide click calls onClose; otherwise calls animateToSlide(slideIndex+1)
  </action>
  <verify>
    - Component renders without TypeScript errors (`npm run build`)
    - All 5 slide visuals render (no broken SVG)
    - Slide animation fires on nav (opacity/transform transition visible)
    - Progress dots reflect active slide; clicking jumps to that slide
    - "Skip" and "Get Started" call onClose
    - "Back" hidden on first slide
  </verify>
  <done>AboutDialog component exists at src/components/ui/about-dialog.tsx with all 5 slides, SVG visuals, animated transitions, and a working onClose prop. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppLayout and Header</name>
  <files>src/components/layout/AppLayout.tsx, src/components/layout/Header.tsx</files>
  <action>
**AppLayout.tsx:**
1. Add state: `const [aboutOpen, setAboutOpen] = useState(false)`
2. Add first-visit effect:
```ts
useEffect(() => {
  if (!localStorage.getItem('rosetta-onboarding-v1')) {
    setAboutOpen(true);
  }
}, []);
```
3. Add close handler:
```ts
const handleAboutClose = useCallback(() => {
  localStorage.setItem('rosetta-onboarding-v1', 'seen');
  setAboutOpen(false);
}, []);
```
4. Pass `onAboutClick={() => setAboutOpen(true)}` to `<Header>`.
5. Render `<AboutDialog open={aboutOpen} onClose={handleAboutClose} />` alongside the existing `<ConfirmDialog>` and `<Toaster>`.
6. Import AboutDialog from `@/components/ui/about-dialog`.

**Header.tsx:**
1. Add `onAboutClick: () => void` to the Header props interface.
2. Wire the existing About button's `onClick` to `onAboutClick`.
3. No other changes to Header.
  </action>
  <verify>
    - `npm run build` passes with no TS errors
    - About button in header opens the dialog
    - Clearing localStorage and reloading auto-opens the dialog
    - After closing once, reloading does not auto-open again
    - Multiple open/close cycles work without stale state
  </verify>
  <done>AppLayout owns open state and first-visit logic. Header About button triggers the dialog. localStorage prevents repeat auto-opens.</done>
</task>


<task type="auto">
  <name>Task 3: E2E tests for onboarding dialog</name>
  <files>e2e/about-dialog.spec.ts</files>
  <action>
Create `e2e/about-dialog.spec.ts`. Use the `freshPage` fixture from `./fixtures`. The fixture clears IDB and `rosetta-ui` but not `rosetta-onboarding-v1`, so each test that needs a clean first-visit state must call `page.evaluate(() => localStorage.removeItem('rosetta-onboarding-v1'))` in a `beforeEach` or inline before reload.

**Helper — clear onboarding flag:**
```ts
async function clearOnboardingFlag(page: Page) {
  await page.evaluate(() => localStorage.removeItem('rosetta-onboarding-v1'));
  await page.reload({ waitUntil: 'networkidle' });
}
```

**Tests:**

1. **Auto-opens on first visit** — clear flag → reload → assert dialog is visible (`getByRole('dialog')`) and first slide heading "Your Data Doesn't Speak the Same Language" is visible.

2. **Does not auto-open on second visit** — clear flag → reload → click "Skip" → reload → assert `getByRole('dialog')` is not visible.

3. **Slide navigation: Next advances slides** — clear flag → reload → click "Next" button → assert second slide heading "A Shared Ontology Changes Everything" is visible.

4. **Slide navigation: Back returns to previous slide** — clear flag → reload → click "Next" → click "Back" → assert first slide heading visible again.

5. **Progress dots jump to slide** — clear flag → reload → click the 3rd progress dot (index 2) → assert third slide heading "Built on Proven, Open Standards" is visible.

6. **Skip closes dialog** — clear flag → reload → click "Skip" → assert dialog not visible.

7. **Get Started closes dialog and sets localStorage** — clear flag → reload → navigate to last slide (click Next 4 times) → click "Get Started" → assert dialog not visible → assert `localStorage.getItem('rosetta-onboarding-v1')` equals `'seen'`.

8. **About button re-opens dialog** — clear flag → reload → click "Skip" → click About button (`getByRole('button', { name: 'About Rosetta' })`) → assert dialog visible.

Use `expect(...).toBeVisible()` / `expect(...).not.toBeVisible()` throughout. Use `{ timeout: 5000 }` on visibility assertions.
  </action>
  <verify>
    - `npx playwright test e2e/about-dialog.spec.ts` passes all 8 tests
    - No test depends on slide animation timing (assert content, not CSS state)
  </verify>
  <done>e2e/about-dialog.spec.ts exists with 8 passing tests covering auto-open, repeat-visit suppression, slide navigation, dot navigation, Skip, Get Started, and About button re-open.</done>
</task>

</tasks>

<verification>
npm run build
npx playwright test e2e/about-dialog.spec.ts
# Manual checks:
# 1. Open app fresh (or clear localStorage) → dialog auto-opens
# 2. Navigate all 5 slides forward and back
# 3. Click progress dots to jump slides
# 4. Click "Get Started" on last slide → dialog closes, no re-open on reload
# 5. Click "About" in header → dialog re-opens
# 6. Click "Skip" on any slide → dialog closes
</verification>

<success_criteria>
- Clicking 'About' in the header opens the onboarding dialog
- First-time visitors see the dialog auto-open on page load
- Dialog shows 5 slides with prev/next navigation and animated transitions
- Progress dots are clickable and reflect the active slide
- Closing via 'Get Started', 'Skip', or X sets localStorage rosetta-onboarding-v1=seen and prevents future auto-opens
</success_criteria>

<output>.planning/phases/12-onboarding-demo/12-01-SUMMARY.md</output>
